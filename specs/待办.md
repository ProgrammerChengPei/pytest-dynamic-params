- pypi用了2FA后登不上了，原因可能是当时我用了VPN，IP地址已经找不回来了。图中是不是让填google auth生成的验证码？google auth是基于时间生成的验证码，这个验证码好像也没用了。怎么才能重新登上。
- .flake8文件是否和某个文件的作用重复了，能否删除

注意：本对话框的内容范围以后也都只能是tools\pytest-dynamic-params\，不能是根目录E:\程培\pytest\multi_sut_test_hub\。

重读tools\pytest-dynamic-params\内的所有文件，回顾当前对话框中的所有历史记录，先更新需求和设计文档经我确认，然后再更新源码和测试代码。

插件功能包含：param_generator, parametrized_fixture, 与原生pytest配合使用（如pytest.mark.parametrize, fixture），等。