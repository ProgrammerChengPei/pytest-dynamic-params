- pypi用了2FA后登不上了，原因可能是当时我用了VPN，IP地址已经找不回来了。图中是不是让填google auth生成的验证码？google auth是基于时间生成的验证码，这个验证码好像也没用了。怎么才能重新登上。
- README.md 问题：
  - 安装命令pip install -e .是不是应该换成pip install pytest-dynamic-params
  - 配置 pytest.ini目前用的是 \[pytest] 并且以 dynamic\_param\_开头，但是装饰器用的是 dynamic\_params 。是否应该改成 \[pytest.dynamic\_params] 然后把配置名称去掉前缀？
- .flake8文件是否和某个文件的作用重复了，能否删除
- docs\STRUCTURE.md中细化tests的结构，细化到tests内的目录和文件即可、更深层的目录和文件不用提及
- 优化docs\usage-guide.md中的示例
- 参考分析.md中的内容（384行静态fixture调用静态参数已经核对完了），更新优化specs，src, tests, examples, 根目录等目录下的文件
- 优化可能存在于很多项目文件中的示例

